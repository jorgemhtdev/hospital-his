<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage
    x:Class="HospitalApp.Views.MasterView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:HospitalApp.Views;assembly=HospitalApp">
    <MasterDetailPage.Master>
        <views:NavView
            Title=" "
            BackgroundColor="{StaticResource AccentColor1}"
            Icon="BurgerMenu" />
    </MasterDetailPage.Master>
    <MasterDetailPage.Detail>
        <NavigationPage
            x:Name="Navigator"
            BarBackgroundColor="{StaticResource BackgroundColor}"
            BarTextColor="{StaticResource FontColor}">
            <x:Arguments>
                <views:HomeView />
            </x:Arguments>
        </NavigationPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>